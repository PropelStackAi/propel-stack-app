import { PagePlaceholder } from '../components/PagePlaceholder';

export function InboxIntelligence() {
  return (
    <PagePlaceholder
      title="Inbox Intelligence"
      sessionNumber={18}
      description="Multi-account email and SMS triage. AI extracts action items, detects bills and deadlines, and surfaces what needs your attention — without you reading every message."
      accent="indigo"
    />
  );
}
