import { PagePlaceholder } from '../components/PagePlaceholder';

export function ParentalControls() {
  return (
    <PagePlaceholder
      title="Parental Controls"
      sessionNumber={9}
      description="Linked child profiles, screen-time schedules, content filters, app usage reports, AI conversation review, six-digit PIN lock, and emergency contacts."
      accent="purple"
    />
  );
}
