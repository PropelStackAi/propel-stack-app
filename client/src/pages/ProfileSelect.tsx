import { PagePlaceholder } from '../components/PagePlaceholder';

export function ProfileSelect() {
  return (
    <PagePlaceholder
      title="Profile Select"
      sessionNumber={24}
      description="Switch between user profiles on a shared device. Each profile has its own data, AI memory, and personalised settings — keeps your life separate from your family's."
      accent="indigo"
    />
  );
}
