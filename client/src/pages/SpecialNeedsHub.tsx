import React from 'react';
import { PagePlaceholder } from '../components/PagePlaceholder';

export function SpecialNeedsHub(): React.ReactElement {
  return (
    <PagePlaceholder
      title="Special Needs Family Support"
      sessionNumber={12}
      description="Family support hub with crisis detection, scroll-lock disclaimer, condition library across nine categories, IEP and 504 toolkit, crisis planning, and a persistent crisis resources panel."
      accent="purple"
    />
  );
}
