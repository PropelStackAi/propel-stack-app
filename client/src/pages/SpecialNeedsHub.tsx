import { PagePlaceholder } from '../components/PagePlaceholder';

export function SpecialNeedsHub() {
  return (
    <PagePlaceholder
      title="Special Needs Family Support"
      sessionNumber={12}
      description="Family support hub with crisis detection (runs BEFORE every AI call), scroll-lock disclaimer (PSAI-SNFS-DISC-v1.0), condition library across nine categories, IEP & 504 toolkit, crisis planning, and a persistent crisis resources panel."
      accent="purple"
    />
  );
}
